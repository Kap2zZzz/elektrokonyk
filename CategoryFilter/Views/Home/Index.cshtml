@*@model CategoryFilter.Models.CategoryModel*@
@*@model IEnumerable<CategoryFilter.Entities.Category>*@

@{
    Layout = "~/Views/Shared/_LayoutMenu.cshtml";
}

@*<div class="container">
    <h2>Категорії товарів</h2>
    <table class="table">
        <tr>
            <th>Id</th>
            <th>Назва категорії</th>
            <th></th>
        </tr>
        @foreach (var item in @Model)
        {
            <tr>
                <td>
                    @item.Id
                </td>
                <td>
                    @item.Name
                </td>
                <td>
                    @Html.ActionLink("Редагувати", "Edit", "Category", new { id = item.Id }, new { @class = "btn btn-warning" }) |
                    @Html.ActionLink("Переглянути", "DetailsCategory", new { id = item.Id }, new { @class = "btn btn-info" }) |
                    @Html.ActionLink("Видалити", "Delete", new { id=item.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </table>
    <p>
        @Html.ActionLink("Додати категорію", "Index", "Category", null, new { @class = "btn btn-primary" })
    </p>
</div>*@

@*@Html.ActionLink("ADMIN", "Index", "Admin", null, new { @class = "btn btn-primary" })*@

    @*<p>
        @Html.ActionLink("Create New Product", "CreateProduct")
    </p>*@

